"use client";
import { useState } from "react";import Link from "next/link";import { useRouter } from "next/navigation";import { createClient } from "@/lib/supabase";
export function AuthForm({mode}:{mode:"login"|"signup"}){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [name,setName]=useState("");const [msg,setMsg]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setMsg("");const supabase=createClient();const res=mode==="login"?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});if(res.error){setMsg(res.error.message);return}router.push("/dashboard");router.refresh()}return <form onSubmit={submit} className="card mx-auto max-w-md rounded-3xl p-8"><h1 className="text-3xl font-bold">{mode==="login"?"Sign in":"Join the Guild"}</h1>{mode==="signup"&&<label className="mt-6 block text-sm">Full name<input className="input mt-2" value={name} onChange={e=>setName(e.target.value)}/></label>}<label className="mt-6 block text-sm">Email<input type="email" required className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block text-sm">Password<input type="password" required minLength={6} className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)}/></label>{msg&&<p className="mt-4 text-amber-200">{msg}</p>}<button className="btn btn-primary mt-6 w-full">{mode==="login"?"Sign in":"Create account"}</button><p className="mt-4 text-sm text-slate-300">{mode==="login"?<>Need an account? <Link className="text-amber-200" href="/signup">Sign up</Link></>:<>Already have access? <Link className="text-amber-200" href="/login">Sign in</Link></>}</p></form>}
