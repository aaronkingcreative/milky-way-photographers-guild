import { adminImageAction } from "@/app/actions";export function AdminImageTable({images}:{images:any[]}){return <div className="card overflow-x-auto rounded-2xl"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-[#2f445d]/30"><tr><th className="p-3">Image</th><th>Status</th><th>Blur</th><th>Reason</th><th>Actions</th></tr></thead><tbody>{images?.map(i=><tr key={i.id} className="border-t border-white/10"><td className="p-3"><div className="font-bold">{i.title}</div><div className="text-[#b8c4d4]">{i.country}, {i.state_or_province}</div></td><td>{i.moderation_status}</td><td>{i.moderation_blur_required?'Yes':'No'}</td><td><form id={`f-${i.id}`} action={adminImageAction}><input type="hidden" name="image_id" value={i.id}/><input className="input" name="moderation_reason" defaultValue={i.moderation_reason||""}/></form></td><td className="flex flex-wrap gap-2 p-3">{['hide','unhide','blur','unblur','review','delete'].map(a=><button key={a} className="btn btn-ghost px-3 py-1 text-xs" form={`f-${i.id}`} name="action" value={a}>{a}</button>)}</td></tr>)}</tbody></table></div>}
