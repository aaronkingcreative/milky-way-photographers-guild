import { toggleReaction } from "@/app/actions";import { REACTION_TYPES } from "@/lib/images";
export function ReactionBar({imageId,reactions}:{imageId:string;reactions:any[]}){const counts=new Map<string,number>(); reactions?.forEach(r=>counts.set(r.reaction_type,(counts.get(r.reaction_type)||0)+1)); return <div className="flex flex-wrap gap-2">{REACTION_TYPES.map(([type,label])=><form key={type} action={toggleReaction}><input type="hidden" name="image_id" value={imageId}/><input type="hidden" name="reaction_type" value={type}/><button className="rounded-full border border-white/15 bg-[#2f445d]/50 px-3 py-1 text-sm hover:border-[#e79f2b]" type="submit">{label} {counts.get(type)||0}</button></form>)}</div>}
