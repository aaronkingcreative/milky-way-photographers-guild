import { addComment } from "@/app/actions";export function CommentList({comments}:{comments:any[]}){return <div className="space-y-3">{comments?.length?comments.map(c=><div className="mw-card-soft p-4" key={c.id}><div className="flex gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-full border border-[#e79f2b]/50 bg-[#06101c] font-display text-[#f0bd66]">G</span><div><p className="whitespace-pre-wrap text-white/86">{c.body}</p><p className="mt-2 mw-meta">Guild feedback · {new Date(c.created_at).toLocaleString()}</p></div></div></div>):<p className="mw-muted">No Guild feedback yet. Be the first to leave a specific, kind note.</p>}</div>}
export function CommentForm({imageId}:{imageId:string}){return <form action={addComment} className="mt-5 space-y-3 rounded-lg border border-[#e79f2b]/20 bg-[#06101c]/45 p-4"><input type="hidden" name="image_id" value={imageId}/><div className="flex flex-wrap gap-2 text-xs"><span className="mw-filter-chip px-3 py-1">What stands out?</span><span className="mw-filter-chip px-3 py-1">What did they do well?</span><span className="mw-filter-chip px-3 py-1">What would you try differently?</span></div><textarea className="mw-textarea" name="body" rows={4} required placeholder="Leave feedback that helps them grow — be specific and kind."/><button className="mw-btn-secondary">Post Feedback</button></form>}
