import { addComment } from "@/app/actions";export function CommentList({comments}:{comments:any[]}){return <div className="space-y-3">{comments?.length?comments.map(c=><div className="rounded-2xl border border-white/10 bg-[#102238] p-4" key={c.id}><p>{c.body}</p><p className="mt-2 text-xs text-[#b8c4d4]">{new Date(c.created_at).toLocaleString()}</p></div>):<p className="text-[#b8c4d4]">No comments yet.</p>}</div>}
export function CommentForm({imageId}:{imageId:string}){return <form action={addComment} className="mt-4 space-y-3"><input type="hidden" name="image_id" value={imageId}/><textarea className="input" name="body" rows={3} required placeholder="Share thoughtful feedback or encouragement..."/><button className="btn btn-secondary">Post comment</button></form>}
