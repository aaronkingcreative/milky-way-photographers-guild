import { ImageCard } from "./ImageCard";export function ImageGrid({images,reactions}:{images:any[];reactions:any[]}){if(!images?.length)return <div className="card rounded-3xl p-10 text-center text-[#b8c4d4]">No Guild images have been submitted yet.</div>;return <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{images.map(i=><ImageCard key={i.id} image={i} reactions={reactions}/>)}</div>}
