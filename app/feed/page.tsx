import Link from "next/link";import { ImageGrid } from "@/components/images/ImageGrid";import { requireAccess } from "@/lib/guards";import { createServerSupabaseClient } from "@/lib/supabase/server";
export default async function Page(){await requireAccess();const supabase=await createServerSupabaseClient();const [{data:images},{data:reactions}]=await Promise.all([supabase.from("guild_images").select("*").is("hidden_at",null).is("deleted_at",null).not("moderation_status","in",'("hidden","deleted")').order("created_at",{ascending:false}).limit(60),supabase.from("image_reactions").select("image_id,reaction_type")]);return <section className="mw-page-wide"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="mw-eyebrow">The Guild Gallery</p><h1 className="mt-2 mw-page-title">Field Reports from the Dark</h1><p className="mt-3 mw-page-subtitle">Every image a night under the Milky Way. React with intent, leave feedback that teaches.</p><div className="mt-5 flex flex-wrap gap-2"><span className="mw-filter-chip px-4 py-2">All Submissions</span><span className="mw-filter-chip px-4 py-2">IOTW Contenders</span><span className="mw-filter-chip px-4 py-2">Hall of Envy</span><span className="mw-filter-chip px-4 py-2">My Field Reports</span></div></div><Link className="mw-btn-primary rounded-sm" href="/submit">+ New Field Report</Link></div><ImageGrid images={images||[]} reactions={reactions||[]}/></section>}
