import Link from "next/link";import { ImageGrid } from "@/components/images/ImageGrid";import { requireAccess } from "@/lib/guards";import { createServerSupabaseClient } from "@/lib/supabase/server";
export default async function Page(){await requireAccess();const supabase=await createServerSupabaseClient();const [{data:images},{data:reactions}]=await Promise.all([supabase.from("guild_images").select("*").is("hidden_at",null).is("deleted_at",null).not("moderation_status","in",'("hidden","deleted")').order("created_at",{ascending:false}).limit(60),supabase.from("image_reactions").select("image_id,reaction_type")]);return <section className="mx-auto max-w-7xl px-5 py-14"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm uppercase tracking-[.25em] text-[#f4bd61]">Member feed</p><h1 className="text-4xl font-bold">Guild Image Feed</h1><p className="mt-3 max-w-3xl text-[#b8c4d4]">Latest Milky Way submissions from active Guild members.</p></div><Link className="btn btn-primary" href="/submit">Submit image</Link></div><ImageGrid images={images||[]} reactions={reactions||[]}/></section>}
