import { requireAccess } from "@/lib/guards";export default async function Page(){await requireAccess();return <section className="mx-auto max-w-7xl px-5 py-14"><h1 className="text-4xl font-bold">Image Feed</h1><p className="mt-4 max-w-3xl text-slate-300">Image sharing is coming soon. This page will become the main Guild feed for Milky Way images, comments, feedback, and weekly recognition.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="starfield card h-56 rounded-2xl"/>)}</div></section>}
