import { AccessNeededPanel,DashboardCard } from "@/components/Cards";import { requireLogin } from "@/lib/guards";
const cards=[['Image Feed','/feed','See member images and feedback.'],['Submit Image','/submit','Prepare future weekly submissions.'],['Weekly Voting','/winners','Voting opens in a later phase.'],['Image of the Week','/winners','Celebrate recognized Guild work.'],['Resources','/resources','PDFs, tips, and field resources.'],['Online Courses','/courses','Photog Adventures learning links.'],['Milky Way Calendar','/resources','Planning tools coming soon.'],['Achievements coming soon','/submit','Future badges for submitted images.']];
export default async function Page(){const {hasAccess}=await requireLogin();return <section className="mx-auto max-w-7xl px-5 py-14"><h1 className="text-4xl font-bold">Guild Dashboard</h1>{hasAccess?<div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{cards.map(([t,h,c])=><DashboardCard key={t} title={t} href={h}>{c}</DashboardCard>)}</div>:<div className="mt-8"><AccessNeededPanel/></div>}</section>}
