import { requireAdmin } from "@/lib/guards";import { createServerSupabaseClient } from "@/lib/supabase/server";
export default async function Page(){await requireAdmin();const supabase=await createServerSupabaseClient();const {data:profiles}=await supabase.from("profiles").select("id,email,display_name,role,created_at").order("created_at",{ascending:false}).limit(25);return <section className="mx-auto max-w-7xl px-5 py-14"><h1 className="text-4xl font-bold">Admin</h1><p className="mt-3 text-[#b8c4d4]">Member and access management shell. Manual grant SQL is documented in README.</p><div className="card mt-8 overflow-hidden rounded-2xl"><table className="w-full text-left text-sm"><thead className="bg-[#2f445d]/30"><tr><th className="p-3">Email</th><th>Display</th><th>Role</th><th>Created</th></tr></thead><tbody>{profiles?.map(p=><tr className="border-t border-white/[0.12]" key={p.id}><td className="p-3">{p.email}</td><td>{p.display_name}</td><td>{p.role}</td><td>{new Date(p.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></section>}
